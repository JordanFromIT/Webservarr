# ==============================================================================
# WebServarr - Direct Dependencies
# ==============================================================================
# This file lists direct (top-level) dependencies only.
# Run `pip-compile requirements.in -o requirements.txt` to resolve and pin
# the full dependency tree including transitive dependencies.
# ==============================================================================

# Web Framework
# TODO: bump fastapi to >=0.115 (pulls starlette >=0.40) to close CVE-2024-47874
#       (starlette multipart DoS). Requires `pip-compile` to re-resolve the tree
#       and an app smoke test — left at 0.109 here to keep the lock reproducible.
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.7  # CVE-2024-24762 (multipart ReDoS) fixed in 0.0.7

# OIDC Authentication
authlib==1.3.1  # CVE-2024-37568 (JWS alg confusion) fixed in 1.3.1
httpx==0.26.0
itsdangerous==2.1.2

# Database
sqlalchemy==2.0.25
aiosqlite==0.19.0

# Redis
redis==5.0.1
hiredis==2.3.2

# Security
# python-jose removed: it was never imported anywhere in the app and carried
# CVE-2024-33663 / CVE-2024-33664 (plus vulnerable ecdsa/rsa/pyasn1 transitives).
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# Rate Limiting
slowapi==0.1.9

# Utilities
python-dotenv==1.0.1
pydantic==2.5.3
pydantic-settings==2.1.0

# Markdown rendering (for news posts)
markdown==3.5.2
bleach==6.1.0

# HTTP client
requests==2.32.3  # CVE-2024-35195 fixed in 2.32.x

# Date/time utilities
python-dateutil==2.8.2

# Docker management
docker==7.1.0

# Web Push notifications
pywebpush>=2.0.0
